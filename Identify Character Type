#include <stdio.h>
#include<ctype.h>

int main() {
    char a;
    scanf("%c",&a);
    if (isalpha(a))
        printf("Alphabet");
    else if (isdigit(a))
     printf("Digit");
    else
        printf("Symbol");

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
